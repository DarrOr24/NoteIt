export function NoteTxt({note}){
    return <section className="note-text">
                <h2>{note.info.title}</h2>
                <p >{note.info.txt}</p>
        </section>
    
                        
                        
}